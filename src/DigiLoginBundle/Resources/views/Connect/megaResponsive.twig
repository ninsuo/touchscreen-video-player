<script type="text/javascript">

$(window).resize(function() {
    resize_{{ rand }}();
});

$(document).ready(function() {
    resize_{{ rand }}();
});

window.resize_{{ rand }} = function() {
    var ratio = ($('{{ container|e('js') }}').width() / 1024);
    {% for line in js %}
        {{ line|raw }}
    {% endfor %}
};

</script>